<div class="col-md-8 offset-md-1">
  <table class="table">
    <tr>
      <td>
        <% @nyanko.each do |nyanko| %>
    <p><%= image_tag nyanko.image.get_image %></p>
    <%= image_tag nyanko.user.get_image(100,100) %>
    <p>投稿者名：<%= nyanko.user.name %></p>

    <p>タイトル：<%= nyanko.title %></p>

    <p>説明：<%= nyanko.body %></p>

</div>
<% end %>